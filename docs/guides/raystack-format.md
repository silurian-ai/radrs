# Raystack format

!!! note "Under construction"

    Reference for the flat raystack layout — `vcps`, `sweeps`, `returns`,
    `activity`, and `fold_size` semantics — is being written.

See the [`radrs.raystack` API reference](../reference/raystack.md) for the
current API surface.
