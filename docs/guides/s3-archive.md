# S3 archive iteration

!!! note "Under construction"

    Guide for `NexradL2ArchiveIter` — multi-cloud iteration with time
    bounds, site filtering, and prefetch — is being written.

To accumulate the volumes an iterator yields into one array rather than
handling them one at a time, see
[Batch iteration and folding](batching.md).

See the [`radrs` API reference](../reference/radrs.md) for the current signature.
